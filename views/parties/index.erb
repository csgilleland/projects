<h2>Parties</h2>

<ul>
  <% @parties.each do |party| %>
  <li>
    <a href="/parties/<%= party.id %>">
      <%= party.name %>
    </a>
  </li>
  <% end %>
</ul>

<a href="/parties/new">Add a new party</a>
